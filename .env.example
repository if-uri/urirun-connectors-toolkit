# urirun-connectors-toolkit currently requires no environment variables.
#
# Put developer-specific values and secrets in .env. That file is ignored and
# must not be committed; this example file remains the public environment
# contract for Doctor and downstream automation.
